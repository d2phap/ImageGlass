export default class Sidebar {
    /**
     * Adds click events to sidebar menu.
     */
    static addEvents(): void;
    /**
     * Set the active menu for sidebar
     */
    static setActiveMenu(tabPageName: string): void;
}
